# Base URL of the FastAPI backend, including the /api/v1 prefix.
# This is the single source of truth for the frontend's backend URL —
# do not hardcode a backend URL anywhere else in the codebase.
#
# Local development (backend run via Docker Compose / uvicorn):
VITE_API_BASE_URL=http://localhost:8000/api/v1
